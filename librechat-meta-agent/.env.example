# ============================================================================
# Meta Agent - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# REQUIRED variables are marked with (REQUIRED)
# ============================================================================

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------
NODE_ENV=production
LOG_LEVEL=info

# Application URLs (update for production)
APP_URL=http://localhost:3000
API_URL=http://localhost:3100
CORS_ORIGINS=http://localhost:3000,http://localhost:80

# For production deployment, use your domain:
# APP_URL=https://meta-agent.your-domain.com
# API_URL=https://meta-agent.your-domain.com/api
# CORS_ORIGINS=https://meta-agent.your-domain.com

# ----------------------------------------------------------------------------
# Database Configuration (PostgreSQL with pgvector)
# ----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres  # (REQUIRED) Change this for production!
POSTGRES_DB=librechat_meta
POSTGRES_PORT=5433
DATABASE_SSL=false

# For production, use strong password:
# POSTGRES_PASSWORD=your-secure-password-here

# ----------------------------------------------------------------------------
# Redis Configuration (Caching & Sessions)
# ----------------------------------------------------------------------------
REDIS_PASSWORD=redis_default_pass  # (REQUIRED) Change this for production!
REDIS_PORT=6380

# For production:
# REDIS_PASSWORD=your-secure-redis-password

# ----------------------------------------------------------------------------
# Security & Authentication
# ----------------------------------------------------------------------------
# (REQUIRED) Generate secure random strings for production
# Use: openssl rand -base64 32

JWT_SECRET=your-jwt-secret-change-this-in-production
SESSION_SECRET=your-session-secret-change-this-in-production
ENCRYPTION_KEY=your-encryption-key-change-this-in-production

# Example generation:
# JWT_SECRET=$(openssl rand -base64 32)
# SESSION_SECRET=$(openssl rand -base64 32)
# ENCRYPTION_KEY=$(openssl rand -base64 32)

# ----------------------------------------------------------------------------
# AI Service API Keys
# ----------------------------------------------------------------------------

# Anthropic Claude (REQUIRED for core functionality)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI (Optional - for GPT models)
OPENAI_API_KEY=your-openai-api-key-here

# Google AI (Optional - for Gemini models)
GOOGLE_API_KEY=your-google-api-key-here

# ElevenLabs (Optional - for voice synthesis)
# Required if ENABLE_VOICE=true
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# Stability AI (Optional - for image generation)
# Required if ENABLE_IMAGES=true
STABILITY_API_KEY=your-stability-api-key-here

# Replicate (Optional - for various AI models)
REPLICATE_API_KEY=your-replicate-api-key-here

# ----------------------------------------------------------------------------
# Google OAuth & Workspace Integration
# ----------------------------------------------------------------------------
# Required for Google authentication and Workspace features
# Get credentials at: https://console.cloud.google.com/apis/credentials

GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_WORKSPACE_DOMAIN=your-workspace-domain.com

# OAuth redirect URI (add this to Google Console):
# http://localhost:3000/auth/google/callback (development)
# https://your-domain.com/auth/google/callback (production)

# ----------------------------------------------------------------------------
# File Upload & Storage
# ----------------------------------------------------------------------------
MAX_FILE_SIZE=104857600  # 100MB in bytes
UPLOAD_DIR=/app/uploads

# For cloud storage (optional):
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1

# ----------------------------------------------------------------------------
# Feature Flags
# ----------------------------------------------------------------------------
# Enable or disable specific features
ENABLE_VOICE=true
ENABLE_IMAGES=true
ENABLE_VIDEO=true
ENABLE_COMPUTER_USE=true
ENABLE_SCHEDULED_TASKS=true
ENABLE_GOOGLE_WORKSPACE=false
ENABLE_VS_CODE_EXTENSION=false

# ----------------------------------------------------------------------------
# Nginx Configuration
# ----------------------------------------------------------------------------
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ----------------------------------------------------------------------------
# Next.js Frontend Configuration
# ----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:3100
NEXT_PUBLIC_WS_URL=ws://localhost:3100
NEXT_PUBLIC_APP_NAME=Meta Agent

# For production:
# NEXT_PUBLIC_API_URL=https://meta-agent.your-domain.com/api
# NEXT_PUBLIC_WS_URL=wss://meta-agent.your-domain.com/ws

# Feature flags for frontend
NEXT_PUBLIC_ENABLE_VOICE=true
NEXT_PUBLIC_ENABLE_IMAGES=true
NEXT_PUBLIC_ENABLE_VIDEO=true

# ----------------------------------------------------------------------------
# Email Configuration (Optional - for notifications)
# ----------------------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=Meta Agent <noreply@your-domain.com>

# ----------------------------------------------------------------------------
# Monitoring & Analytics (Optional)
# ----------------------------------------------------------------------------
# SENTRY_DSN=your-sentry-dsn
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# MIXPANEL_TOKEN=your-mixpanel-token

# ----------------------------------------------------------------------------
# Performance & Scaling
# ----------------------------------------------------------------------------
# Node.js memory limits (in MB)
# NODE_OPTIONS=--max-old-space-size=4096

# Worker processes
# WORKER_PROCESSES=4

# Database connection pool
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# Redis connection pool
# REDIS_POOL_MIN=2
# REDIS_POOL_MAX=10

# ----------------------------------------------------------------------------
# Development Settings
# ----------------------------------------------------------------------------
# Uncomment for development mode

# NODE_ENV=development
# LOG_LEVEL=debug
# NEXT_TELEMETRY_DISABLED=1

# ----------------------------------------------------------------------------
# Production Deployment Notes
# ----------------------------------------------------------------------------
# 1. Change all passwords and secrets
# 2. Use HTTPS in production (set up SSL certificates)
# 3. Configure your domain in APP_URL, API_URL, and CORS_ORIGINS
# 4. Set up proper database backups
# 5. Configure monitoring and logging
# 6. Review and enable only needed features
# 7. Set up proper firewall rules
# 8. Use environment-specific .env files
# 9. Never commit .env files to version control
# 10. Use secrets management for sensitive data in production

# ----------------------------------------------------------------------------
# Quick Start Commands
# ----------------------------------------------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and fill in your values
# 3. Generate secrets:
#    - JWT_SECRET: openssl rand -base64 32
#    - SESSION_SECRET: openssl rand -base64 32
#    - ENCRYPTION_KEY: openssl rand -base64 32
# 4. Add your Anthropic API key (minimum requirement)
# 5. Deploy: ./scripts/deploy.sh
